# Copy to .env and fill in your keys
# ⚠️ NEVER commit .env to git - it contains sensitive keys!

# ============================================================
# GEMINI API CONFIGURATION
# ============================================================
# Get your key: https://aistudio.google.com/app/apikey
#
# SECURITY RECOMMENDATIONS:
# 1. Go to Google Cloud Console → APIs & Services → Credentials
# 2. Click on your API key → Add restrictions:
#    - API restrictions: Only "Generative Language API"
#    - Set daily quota limits (e.g., 1000 requests/day)
# 3. Rotate keys periodically, especially after demos
#
GEMINI_API_KEY=
GEMINI_MODEL=gemini/gemini-2.5-flash

# ============================================================
# GITHUB OAUTH (Optional - for private repo access)
# ============================================================
# Create OAuth App: https://github.com/settings/developers
# Set callback URL: http://localhost:8000/auth/github/callback
#
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_REDIRECT_URI=http://localhost:8000/auth/github/callback
FRONTEND_URL=http://localhost:3000
